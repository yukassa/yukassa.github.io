---
name: "Crowd Simulation & Optimization light ver6"
summary: 仮想的な人流の挙動を観察できる人流シミュレータ。通行人の移動ルールや障害物である壁オブジェクトを設定することで、通行人が形成する人流の変化をリアルタイムで観察・比較できます。さらにGA（遺伝的アルゴリズム）でシミュレーション結果を評価し、人流を改善させる壁オブジェクトの配置最適化を行うことができます。
architecture: "Real-Time Simulation / Optimization"
technologies: ["Go", "JavaScript", "p5.js", "Chart.js", "WebSocket", "HTTP API"]
projectType: "personal"
order: 1
details:
  - "Goで通行人の移動ロジックを計算し、p5.jsでリアルタイムに可視化。人数・歩行速度・壁などの条件を変更し、集団の流れがどう変化するかを観察可能。"
  - "目的地までの経路選択と、周囲の人を避ける局所回避を独立に切り替え、シミュレーション結果を比較。平均到達時間・最大混雑度・到達率で結果を定量評価。"
  - "描画を省略した評価用シミュレーションをGAから繰り返し実行し、人流を改善できる壁オブジェクトの配置場所を探索。"
---

import ZoomImage from "../../components/ZoomImage.astro";

## 概要

仮想的な通行人がスタート地点からゴール地点へ移動する様子をブラウザ上で観察し、**移動ルールや空間条件の違いによって集団の流れがどう変化するかを比較・評価できる人流シミュレータ**です。

シミュレーションでは、 **「目的地までどの道筋で進むか」を決める処理（大域経路探索）** と、 **「近くの人との衝突をどう避けるか」を決める処理（局所回避）** を分離しています。それぞれ2方式を用意し、**同じマップ、エージェント数、移動パラメータ、乱数条件、評価時間**を使って4通りの組み合わせを比較できます。

ブラウザ上では人流をリアルタイムに観察しながら、エージェント数や歩行速度などの条件変更や、壁オブジェクトを配置したり、対向流・交差流などのシナリオを切り替えることができます。また、画面描画を省略した評価用シミュレーションで **平均到達時間・最大混雑度・到達率** を計測し、移動モデルや壁配置による違いを数値で比較します。

さらに、配置済みの壁オブジェクトについて、 **「どこへ配置すると、より短時間で到達しやすく、混雑や未到達を抑えられるか」** をGA（遺伝的アルゴリズム）で探索します。人流を可視化するだけでなく、**条件変更 → 観察 → 定量評価 → 配置最適化**までを一つの検証フローとして扱える構成にしています。

## 背景・目的

創発現象やマルチエージェントシステムに関心があり、**個々のエージェントに単純なルールを与えたとき、その相互作用から集団全体としてどのような振る舞いが生まれるのかを観察したい**と考えたことが、このプロジェクトの出発点です。

まずは日常でもイメージしやすく、複数の人が互いに影響しながら移動する **人流** を題材にしました。

単に人が動く様子を可視化するだけではなく、**移動モデルや壁配置を変更し、平均到達時間・最大混雑度・到達率という共通の指標で比較できること**を重視しています。

さらに、シミュレーションの評価結果を用いた壁配置最適化へつなげました。GAによる探索では、候補ごとに描画を行う必要がないため、同じ移動ロジックを使った描画なしの評価用シミュレーションを繰り返し実行しています。

## システム構成（⭐️図をアピールしたい要点に絞ってシンプルに）

<ZoomImage
  src="/crowd-simulation-architecture-simple-ja-v2.svg"
  alt="人流シミュレーションと配置最適化のシステム構成"
/>

_Figure: Go側でリアルタイムシミュレーション・評価・配置最適化を実行し、ブラウザ側で可視化・条件編集・結果比較を行う構成。_

| レイヤー             | 技術                        | 主な役割                                                                                                     |
| :------------------- | :-------------------------- | :----------------------------------------------------------------------------------------------------------- |
| **シミュレーション** | Go                          | 約20Hzの状態更新、経路探索、局所回避、空間条件の管理、描画なし評価シミュレーション、GA配置最適化             |
| **可視化・分析UI**   | JavaScript, p5.js, Chart.js | エージェント描画、壁・スタート/ゴール編集、パラメータ操作、評価・比較結果の可視化                            |
| **通信**             | WebSocket, HTTP API         | リアルタイム状態配信と編集操作、シミュレーション設定値の評価・モデル比較・GA配置最適化の実行・最良配置の適用 |

シミュレーション・経路探索・評価・配置最適化はGo側で実行し、ブラウザ側は状態の可視化と操作を担当します。

リアルタイムのエージェント状態やマップ編集には **WebSocket（双方向のリアルタイム通信）**、評価・アルゴリズム比較・GA実行などの単発処理には **HTTP API** を使用します。
通行人の位置は約20Hzで更新し、リアルタイムの状態をWebSocketでブラウザへ送ります。一方、現在の条件を数値評価したり、複数の移動モデルを比較したり、GAで配置候補を探索したりする処理はHTTP APIとして実行します。

## 技術的な課題と工夫

### 1. 「経路を選ぶ処理」と「人を避ける処理」の分離

人の移動を考えると、

1. **目的地まで、どの道筋で進むか（大域経路探索）**
2. **近くに他の人がいたとき、どう避けるか（局所回避）**

は別の問題です。

そこで、この2つを別々の処理として実装し、組み合わせを切り替えられるようにしました。

#### 目的地までの経路を選ぶ方法（大域経路探索）

| 手法           | 概要                                                                                                                       |
| :------------- | :------------------------------------------------------------------------------------------------------------------------- |
| **A\***        | 現在位置からゴールまで、壁を避ける経路を探索し、その経路に沿って移動する                                                   |
| **Flow Field** | 各地点からゴールへ進む方向をあらかじめ計算・マッピングしておく方式。多数のエージェントがマッピングされた方向に沿って進む。 |

#### 近くの人を避ける方法（局所回避）

| 手法                       | 概要                                                                 |
| :------------------------- | :------------------------------------------------------------------- |
| **Force-based separation** | 近くのエージェントから離れる向きに反発を加え、近距離では反発を強める |
| **Social Force Model**     | 周囲の人との距離に応じた指数的な反発を加える簡略モデル               |

大域経路探索と局所回避を別々の設定として持つことで、**2種の大域経路探索 × 2種の局所回避**の4通りを自由に組み合わせられる構成としています。

壁オブジェクトの追加・削除・移動時には通行可能領域を更新し、経路探索アルゴリズムを再計算することで、変更後の空間へ追従させます。

局所回避では、同じ方向へ進む人だけでなく、**異なる人流に属する通行人も互いに回避対象**としています。対向する人流や交差する人流でも周囲の人に反応するため、異なる人流同士の相互作用を観察できます。

### 2. リアルタイム観察と繰り返し評価の分離

ブラウザ上で人の動きを観察する処理と、多数の条件を繰り返し評価する処理では、必要な処理が異なります。

- **リアルタイムシミュレーション**  
  Go側で約50 ms間隔（20 Hz）でシミュレーションを更新し、計算済みのエージェント状態をWebSocketでブラウザへ配信します。
- **描画を省略した評価用シミュレーション**  
  条件を評価するときは、ライブ表示と同じ移動ロジックを再利用しながら、WebSocket配信や画面描画を行わず、固定時間だけシミュレーションを進めます。
- **HTTP APIによる評価・探索**  
  現在配置の評価、4通りのアルゴリズム比較、GAによる配置最適化などの単発処理はHTTP APIとして分離します。

これにより、観察用と評価用で移動ロジックを二重実装せず、同じモデルをリアルタイム表示と繰り返し実験の両方に利用できる構成としています。

### 3. 比較可能な評価指標と再現性

シミュレーションを見て「こちらの方が流れが良さそう」と判断するだけでは、条件ごとの差を比較しにくくなります。

そこで、以下の3指標を共通の評価軸として使っています。

| 指標             | 何を見るか                                                                         |
| :--------------- | :--------------------------------------------------------------------------------- |
| **平均到達時間** | 全体として目的地へどのくらい早く到達できたか。未到達者は評価時間の上限値として集計 |
| **最大混雑度**   | 評価中に、1つのグリッドセルへ同時に集まった人数の最大値                            |
| **到達率**       | 評価時間内に目的地へ到達できた人の割合                                             |

平均到達時間を計算する際は、評価時間内にゴールへ到達できなかった通行人も集計対象に含めます。未到達者を単純に除外すると、少数の通行人だけが早く到着し、多くの通行人が到達できなかった条件でも「平均到達時間が短い」と評価されてしまうためです。

そのため、未到達者には評価時間の上限値を到達時間として与えています。

最大混雑度は、評価中に1つのグリッドセルへ同時に集まった人数の最大値です。実際の人口密度（人 / ㎡）を推定するものではなく、 **シミュレーション内でどの条件がより局所的な混雑を生みやすいかを比較するための簡易指標** として使っています。

#### 乱数条件の分離

シミュレーションでは、通行人の初期位置などに乱数を使います。また、GAによる配置最適化にも乱数が使われます。

乱数条件が毎回変わると、結果の違いが「移動モデルの違い」なのか「たまたま初期位置が違っただけ」なのか判断しづらくなります。

そこで、乱数を以下の2つに分けています。

- `simulation_seed`：通行人の初期位置など、シミュレーション側の乱数条件
- `optimizer_seed`：GAの候補生成・選択・交叉・突然変異など、探索側の乱数条件

同じシナリオ・設定・seedを指定することで、同じ条件から評価や探索をやり直せるようにしています。

### 4. シミュレーション結果を使った壁配置最適化

人流を観察・評価するだけでなく、その結果を使って壁配置を最適化できるようにしました。

GA（遺伝的アルゴリズム）は、複数の配置候補を作り、それぞれを評価しながら、より評価の良い候補を残して新しい候補を作る処理を繰り返します。

本システムでは、候補となる壁配置ごとに描画なしで人流シミュレーションを実行し、

- 到達までに時間がかかりすぎないか
- 強い混雑が起きていないか
- 到達できない人が増えていないか

をもとに配置の良し悪しを判定します。

探索後は、**探索開始時の現在配置**と探索で得られた最良候補について3指標を並べて比較し、ユーザーが選択した場合のみ最良候補をライブマップへ適用します。

```text
壁配置の候補を生成
      ↓
描画なしで人流シミュレーションを実行
      ↓
配置の評価値（Fitness）を計算
      ↓
評価のよい候補を残して新しい候補を生成
      ↓
繰り返し
```

#### 壁配置の制限

壁を自由にどこへでも移動できると、単に人の進路から遠ざける配置が有利になりやすくなります。

そこで、**配置可能領域（Placement Zone）**を設定し、その中だけで壁を移動させます。

また、

- 壁同士が重ならない
- 出発地・目的地と重ならない
- マップ外へ出ない
- いずれかの人流について目的地までの経路を完全に塞がない
- 元の壁形状を変えない
- 基準位置から一定範囲内で移動する

といった制約を設けています。

#### 補足：Fitness

GA では **Fitness** という評価値を設定し、Fitness の値をもとにして GA による計算結果を評価します。
今回の場合、**Fitnessは、1つの壁配置がどの程度よいかを候補同士で比較するための評価値**ということになります。
本システムで設定した Fitness は以下の通りで、値が小さいほど良い配置としています。

```text
Fitness = 平均到達時間 + 混雑ペナルティ + 未到達ペナルティ
```

## このプロジェクトで重視したこと

本プロジェクトでは、

**移動ルールをモデル化する → ブラウザで集団挙動を観察する → 条件を変えて同じ指標で比較する → シミュレーション結果を使って環境配置を探索する**

という一連の検証フローを、一つのシステムとして扱います。

単一のアルゴリズムを実装することではなく、**経路探索と局所回避を切り替え可能な形で実装し、挙動を定量評価し、その結果をGAによる壁配置最適化へ接続するところまでソフトウェアとして構成すること**を、このプロジェクトの中心に置いています。

## 現在の状況・制限事項

現在は、**一方向流・対向流・交差流のシナリオ、4通りの移動モデル比較、3指標による定量評価、配置可能領域内でのGAによる制約付き壁配置最適化**を中核機能として実装しています。

本シミュレータは実人流データによるパラメータ校正・妥当性検証を行っておらず、実世界の人流予測や安全判断を目的としたものではありません。最大混雑度は、条件間を比較するための簡易指標として扱っています。
