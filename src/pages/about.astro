---
import BaseLayout from '../layouts/BaseLayout.astro';
import projects from '../data/projects.json';

const featuredProjects = projects.slice(0, 2);
---

<BaseLayout title="About Me — yukassa" description="クリエイティブデベロッパー yukassa のプロフィールと提供価値。">
  <article class="page">
    <header class="page__hero container">
      <p class="page__eyebrow">About Me</p>
      <h1>ものづくりの熱量をユーザー体験へ変換する人</h1>
      <p class="page__lead">
        プロダクトのフェーズに合わせて、調査・設計・実装を横断しながら「伝わる体験」を形にすることが得意です。
        デザインシステムとフロントエンドを軸に、チームが継続的に改善を届けられる仕組みづくりをしています。
      </p>
      <dl class="facts">
        <div>
          <dt>拠点</dt>
          <dd>Tokyo / Remote</dd>
        </div>
        <div>
          <dt>得意領域</dt>
          <dd>DesignOps, Frontend, Product Discovery</dd>
        </div>
        <div>
          <dt>最近のフォーカス</dt>
          <dd>Design System, Stripe 自動化, Astro による高速配信</dd>
        </div>
      </dl>
    </header>

    <section class="section container">
      <h2>大切にしていること</h2>
      <ul class="values">
        <li>課題の解像度が合っているかを問い直し、チームで「なぜ」を共有する。</li>
        <li>ユーザーの声とデータを往復しながら、仮説検証を小さく素早く回す。</li>
        <li>誰もが編集しやすいドキュメントとデザインシステムで、改善のボトルネックを減らす。</li>
      </ul>
    </section>

    <section class="section container">
      <div class="section__header">
        <div>
          <p class="page__eyebrow">Selected Projects</p>
          <h2>まず見てほしいプロジェクト</h2>
        </div>
        <a class="section__link" href="/projects">プロジェクト一覧へ</a>
      </div>
      <div class="featured-projects">
        {featuredProjects.map((project) => (
          <article class="featured-project">
            <img src={project.thumbnail} alt={`${project.title} のイメージ`} loading="lazy" />
            <div class="featured-project__body">
              <h3>{project.title}</h3>
              <p class="featured-project__tagline">{project.tagline}</p>
              <p>{project.summary}</p>
              <ul>
                {project.highlights.slice(0, 2).map((item) => (
                  <li>{item}</li>
                ))}
              </ul>
              <a class="featured-project__link" href={`/projects#${project.slug}`}>詳細を見る</a>
            </div>
          </article>
        ))}
      </div>
    </section>
  </article>
</BaseLayout>

<style>
  .page {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .page__hero {
    padding-top: var(--space-lg);
    display: grid;
    gap: 2rem;
  }

  .page__eyebrow {
    margin: 0;
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent);
  }

  .page__lead {
    margin: 0;
    max-width: 720px;
    color: var(--muted);
  }

  .facts {
    display: grid;
    gap: 1.25rem;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    margin: 0;
  }

  .facts div {
    background: var(--surface);
    border-radius: var(--radius-md);
    padding: 1.25rem;
    box-shadow: var(--shadow);
  }

  .facts dt {
    margin: 0 0 0.25rem;
    font-weight: 600;
    color: var(--muted);
  }

  .facts dd {
    margin: 0;
    font-weight: 600;
  }

  .section {
    display: flex;
    flex-direction: column;
    gap: 1.75rem;
  }

  .values {
    margin: 0;
    padding-left: 1.25rem;
    display: grid;
    gap: 0.85rem;
    color: var(--muted);
  }

  .section__header {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 1.5rem;
    flex-wrap: wrap;
  }

  .section__link {
    color: var(--muted);
    font-weight: 600;
    text-decoration: none;
  }

  .section__link:hover,
  .section__link:focus {
    color: var(--accent);
  }

  .featured-projects {
    display: grid;
    gap: 2rem;
  }

  .featured-project {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: 2rem;
    background: var(--surface);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow);
  }

  .featured-project img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .featured-project__body {
    padding: 2rem;
    display: grid;
    gap: 1rem;
    align-content: center;
  }

  .featured-project__tagline {
    margin: 0;
    color: var(--accent);
    font-weight: 600;
  }

  .featured-project__body ul {
    margin: 0;
    padding-left: 1.25rem;
    color: var(--muted);
  }

  .featured-project__link {
    justify-self: flex-start;
    color: var(--accent);
    font-weight: 600;
    text-decoration: none;
  }

  @media (max-width: 960px) {
    .featured-project {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .page__hero {
      padding-top: var(--space-md);
    }

    .featured-project__body {
      padding: 1.5rem;
    }
  }
</style>
